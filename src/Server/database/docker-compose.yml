version: "3.0"

services:
  database:
    image: postgres:latest
    container_name: database
    ports:
      - "5432:5432"
    volumes:
      - /var/storage/postgres:/var/lib/postgresql/data
    restart: always
    networks:
      - weather_station

networks:
  weather_station:
    driver: bridge
